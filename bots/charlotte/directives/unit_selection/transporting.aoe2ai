#if goal full-water-map 1 and soldier-count > 5 and warboat-count >= 20
  #if unit-type-count-total transport-ship < 4
    goal produce-transport-ships = 1
  #else
    goal produce-transport-ships = 0
  #end if
#else
  goal produce-transport-ships = 0
#end if
